import styled from 'styled-components';

const FavoriteComp = styled.div`
  .container {
    width: 900px;
    height: 800px;
    margin: 0 auto;
    padding: 0px;
    border-radius: 10px;
  }
`;

export default FavoriteComp;
